# Pipeline runtime dependencies (scripts/, tools/).
#
# This file declares supported ranges for maintainers. Reproducible runs and CI
# install `requirements.lock`, which is generated with hashes from these ranges.
# Majors stay capped because pandas and pyarrow make breaking changes across them.
#
# Install with an x64 Python 3.12 — a 32-bit interpreter has no pyarrow wheel and
# will fail to build one:
#     python -m pip install -r requirements.txt
# Exact environment:
#     python -m pip install --require-hashes -r requirements.lock

numpy>=2.5.2,<3
pandas>=3.0.5,<4
pyarrow>=25.0.1,<26
PyYAML>=6.0.3,<7
