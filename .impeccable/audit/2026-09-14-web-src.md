# Impeccable audit baseline, 14 September 2026, target web/src

Run on Opus before any design change (Phase 1.4 of docs/DESIGN_ROADMAP.md).
Health score 15/20 (Good). 0 P0, 2 P1, 4 P2, 4 P3. Detector: one verified
warning (BasketDrawer.svelte:322). npm run check and npm run lint exit 0.

| Dimension | Score | Key finding |
|---|---|---|
| Accessibility | 3 | Targets under 24px (WCAG 2.2 AA 2.5.8): `Methods` nav link 18.6px, disclosure summary and provenance link 22.9px, theme toggle ~22px |
| Performance | 3 | MapLibre imported at mount on /actors, map sits ~3,490px below the fold |
| Responsive | 3 | No overflow at 375/390/464px; touch targets are the only gap |
| Theming | 3 | Semantic map hard-codes 9 hexes (six are light-theme register hues) and ignores the theme switch; diamond hard-coded #14171a |
| Integrity | 3 | One detector warning; `--paper-2` undefined (Figure.svelte:305); register hue used for warning rails (chronology:1579, language:1495) |

P1: semantic/+page.svelte:67-76,142 palette -> derive from $colours / categoricalNeutral(); targets < 24px in +layout.svelte:312-318, ThemeToggle.svelte:41-52, app.css .data-table summary, Figure.svelte:198.
P2: CountryMap.svelte:225,243 eager MapLibre import; global reduced-motion kill in app.css; register conveyed by colour alone in reader/[meeting]/+page.svelte:518-523; --reg-contentious as a UI warning colour.
P3: --paper-2 undefined; BasketDrawer side-tab; four font faces fetched on text-only routes (italic 127KB, only Roman preloaded); uneven masthead row (Methods lacks a provenance badge).

Positives: ECharts code-split per route and registered per module; basket is a native <dialog>; ARIA consistent (role=group + aria-labelledby on segmented controls, aria-pressed, implicit labels on ~30 selects, fieldset/legend in ScopeControl); theme.ts re-reads tokens on theme change via MutationObserver; no will-change; ResizeObservers rAF-guarded.

False positives noted by the auditor: 16 "undefined" custom properties are local component APIs set via style:--x at call sites; only --paper-2 is real.
