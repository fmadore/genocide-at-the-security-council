// 6,595 meetings would be 6,595 prerendered pages showing text that is already
// fetched as JSON. The adapter's SPA fallback serves this route instead.
export const prerender = false;
export const ssr = false;
